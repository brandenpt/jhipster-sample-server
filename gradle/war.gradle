apply plugin: "war"

bootWar {
    mainClassName = "pt.branden.brandenportal.BrandenPortalServerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
